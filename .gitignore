node_modules/
.next/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Lock file contains corporate artifactory URLs — regenerate with npm install
package-lock.json

# Dev output / AI bundle files (not needed in source control)
dev-output.log
project_bundle_*.txt
project_bundle_config.txt

# Playwright artifacts (regenerated by running tests)
playwright-report/
test-results/

# AI tool skills — generated, not source code
# Update by running: cd .ai-tools && npx skills add --all https://docs.stripe.com
.ai-tools/

# AI tool MCP config — contains credentials, must not be committed
.ai/

