node_modules/
.next/
coverage/
.DS_Store
*.log

# TypeScript / Next.js build artifacts (auto-generated)
*.tsbuildinfo
next-env.d.ts

.env*
!.env.example

# Temp reorg/inventory artifacts (not source)
md_inventory.txt
md_reorg_*.log
md_backup_*/

# Personal job-application material — keep private, do not publish
COVER_LETTER.md
PROJECT_ONEPAGER.md

# Lock file not committed — run `npm install` to generate it locally
package-lock.json

# Local npm config (may contain private/registry settings)
.npmrc

# Dev output / AI bundle files (not needed in source control)
dev-output.log
project_bundle_*.txt
project_bundle_config.txt

# Playwright artifacts (regenerated by running tests)
playwright-report/
test-results/

# AI tool skills — generated, not source code
# Update by running: cd .ai-tools && npx skills add --all https://docs.stripe.com
.ai-tools/

# AI tool MCP config — contains credentials, must not be committed
.ai/

